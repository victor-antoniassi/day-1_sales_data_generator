[project]
name = "neon-postgresql"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2.11",
    "python-dotenv>=1.1.1",
]
