# =============================================================================
# Neon Database Configuration
# =============================================================================
# Required: Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control

# Required: Neon organization ID (found in Neon console)
NEON_ORG_ID=org-your-org-id-here

# Required: Neon project ID (found in Neon console)
NEON_PROJECT_ID=your-project-id-here

# Required: Database name where Chinook data is stored
# This MUST match the database name you created in Neon (e.g., chinook_db, chinook)
# See README section "Setting Up Your Neon Database" for details
NEON_DATABASE=chinook_db

# Optional: Specific database role/user to connect with
# If not specified, uses the default role for the project
# NEON_ROLE=your_role_name

# Optional: Specific branch to connect to
# If not specified, uses the default branch (usually 'main')
# NEON_BRANCH=main

# =============================================================================
# Application Configuration
# =============================================================================

# Optional: Logging level for the application
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# =============================================================================
# Optional: PostgreSQL Environment Variables (for development/testing)
# =============================================================================
# These variables are NOT required by the simulator but are useful for:
# - Manual database access via psql without typing full connection strings
# - Quick testing and debugging
# - Development workflows
# - AI/LLM assistants testing the database
#
# If set, you can use psql directly without connection strings:
#   psql -c "SELECT COUNT(*) FROM \"Invoice\";"
#
# Instead of:
#   psql "postgresql://user:pass@host/db" -c "SELECT COUNT(*) FROM \"Invoice\";"
#
# PGHOST=ep-your-endpoint.region.aws.neon.tech
# PGDATABASE=chinook_db
# PGUSER=your-username
# PGPASSWORD=your-password
# PGSSLMODE=require
# PGCHANNELBINDING=require

# =============================================================================
# Usage Instructions
# =============================================================================
#
# 1. Copy this file to .env in the same directory:
#    cp .env.example .env
#
# 2. Fill in your NEON_ORG_ID, NEON_PROJECT_ID, and NEON_DATABASE
#    Get these from: https://console.neon.tech/
#
# 3. (Optional) Uncomment and set other parameters as needed
#
# 4. Run the simulator:
#    echo "10" | uv run main.py simulate
#
